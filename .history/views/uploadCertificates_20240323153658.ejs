<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Excel Sheet</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css"/>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="/uploadpage.css">
</head>
<body>
    <div class="dashboard-container">
        <div class="top-bar">
            <h1>Upload Excel Sheet</h1>
            <form id="logoutForm" action="/logout" method="get">
                <button type="submit"><i class="material-icons">logout</i> Log Out</button>
            </form>
            <form action="/" method="get">
                <button type="submit"><i class="material-icons">back</i> Back</button>
            </form> 
        </div>
        
        <form id="uploadForm" class="upload-form" enctype="multipart/form-data">
            <label for=""></label>
            <input type="file" id="excelFileInput" accept=".xls,.xlsx" required>
            <input type="file" id="templateFileInput" accept=".pdf,.jpg,.jpeg,.png" required>
            <label for="id">Certificate Unique ID:</label>
            <input type="text" id="id" required>
            <label for="student">Student Name:</label>
            <input type="text" id="student" required>
            <label for="event">Event Name:</label>
            <input type="text" id="event" required>
            <label for="date">Date:</label>
            <input type="date" id="date" required>
            <button type="button" onclick="processFiles()"><i class="material-icons">cloud_upload</i> Upload</button>
        </form>

        <div id="templatePreview" style="display: none;"></div>
    </div>

    <script>
        function processFiles() {
            // Retrieve form data
            var excelFile = document.getElementById('excelFileInput').files[0];
            var templateFile = document.getElementById('templateFileInput').files[0];
            var id = document.getElementById('id').value;
            var student = document.getElementById('student').value;
            var event = document.getElementById('event').value;
            var date = document.getElementById('date').value;

            // You can now handle the uploaded files accordingly
            // For simplicity, let's assume you just want to log their details for now
            console.log('Excel File:', excelFile);
            console.log('Template File:', templateFile);
            console.log('Certificate Unique ID:', id);
            console.log('Student Name:', student);
            console.log('Event Name:', event);
            console.log('Date:', date);

            // Now you can proceed to process the uploaded files and extract information
            // You can use libraries like OpenCV.js or implement your own logic to extract text from coordinates
            // Then you can proceed with uploading the extracted information
        }
    </script>
</body>
</html>
