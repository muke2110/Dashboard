<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css"/>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="/login.css">
    <title>Login</title>
</head>
<body>
    <h1>LOGIN PAGE</h1>

    <form action="/users/login" method="post">
        <div id="errors-container"></div>

        <label>User Type</label>
        <select name="role" required>
            <option value="student">Student</option>
            <option value="admin">Admin</option>
        </select>
        <label>Roll Number</label>
        <input type="Roll Number" required name="roll_number" />
        <label>Password</label>
        <input type="password" required name="password" />

        <button type="submit">Login</button>
    </form>

    <a href="/signup">Not a user?</a>

    <script>
        // Assume that the errors variable is defined in your script
        var errors = /* Populate this array with error messages */;
        var errorsContainer = document.getElementById('errors-container');

        if (typeof errors !== 'undefined') {
            var errorsList = document.createElement('ul');

            errors.forEach(function (error) {
                var listItem = document.createElement('li');
                listItem.textContent = error.message;
                errorsList.appendChild(listItem);
            });

            errorsContainer.appendChild(errorsList);
        }
    </script>
</body>
</html>
