<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Excel Sheet</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css"/>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="/uploadpage.css">
</head>
<body>
    <div class="dashboard-container">
        <div class="top-bar">
            <h1>Upload Excel Sheet</h1>
            <form id="logoutForm" action="/logout" method="get">
                <button type="submit"><i class="material-icons">logout</i> Log Out</button>
            </form>
            <form action="/" method="get">
                <button type="submit"><i class="material-icons">back</i> Back</button>
            </form> 
        </div>
        
        <form id="uploadForm" class="upload-form">
            <input type="file" id="fileInput" accept=".xls,.xlsx" required>
            <label for="template">Select Template:</label>
            <select id="template">
                <option value="pdf">PDF</option>
                <option value="image">Image</option>
            </select>
            <label for="id">Certificate Unique ID:</label>
            <input type="text" id="id" required>
            <label for="student">Student Name:</label>
            <input type="text" id="student" required>
            <label for="event">Event Name:</label>
            <input type="text" id="event" required>
            <label for="date">Date:</label>
            <input type="date" id="date" required>
            <button type="button" onclick="processTemplate()"><i class="material-icons">cloud_upload</i> Upload</button>
        </form>

        <div id="templatePreview" style="display: none;"></div>
    </div>

    <script>
        function processTemplate() {
            // Retrieve form data
            var file = document.getElementById('fileInput').files[0];
            var templateType = document.getElementById('template').value;
            var id = document.getElementById('id').value;
            var student = document.getElementById('student').value;
            var event = document.getElementById('event').value;
            var date = document.getElementById('date').value;

            // Read the file and display the template
            var reader = new FileReader();
            reader.onload = function(e) {
                var templatePreview = document.getElementById('templatePreview');
                templatePreview.innerHTML = '<img src="' + e.target.result + '" alt="Template Preview">';
                templatePreview.style.display = 'block';

                // Now you can add functionality to allow the user to select coordinates on the template
                // For simplicity, let's assume the user manually inputs the coordinates for now
                var xCoordinate = prompt('Enter X Coordinate:');
                var yCoordinate = prompt('Enter Y Coordinate:');
                
                // Now you can use the uploaded file, template type, and coordinates to extract information
                // You can use libraries like OpenCV.js or implement your own logic to extract text from coordinates
                // Then you can proceed with uploading the extracted information
            };
            reader.readAsDataURL(file);
        }
    </script>
</body>
</html>
